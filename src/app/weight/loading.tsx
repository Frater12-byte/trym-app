import { LoadingState } from "@/components/LoadingState";

export default function Loading() {
  return (
    <LoadingState
      eyebrow="Weight log"
      title="Loading..."
      variant="dashboard"
    />
  );
}
